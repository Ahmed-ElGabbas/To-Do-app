/**
 * Task priority levels. Values match the Tasko Flutter client exactly
 * (`high` | `medium` | `low`) so client and server stay in sync.
 */
export enum TaskPriority {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}
