/**
 * App languages supported by the Tasko Flutter client (see
 * `lib/core/localization/app_localizations.dart`). The server mirrors this so
 * user preferences can be validated and synced across devices.
 */
export enum AppLanguage {
  EN = 'en',
  AR = 'ar',
  FR = 'fr',
}
