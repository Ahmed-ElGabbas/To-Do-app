/** Name of the single shared BullMQ queue used for Phase 3 async jobs. */
export const TASKO_QUEUE = 'tasko';

/** Job name for domain events emitted by write services. */
export const TASK_EVENT_JOB = 'task-event';

/** Job name for push-notification delivery. */
export const PUSH_JOB = 'push';
